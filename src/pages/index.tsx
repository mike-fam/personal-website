import { Box } from "@chakra-ui/react";
import React from "react";

type Props = Record<string, never>;

const Index: React.FC<Props> = () => {
    return (
        <Box bg="tomato" w="90%" mx="auto">
            test
        </Box>
    );
};

export default Index;
